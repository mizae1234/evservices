# System Flow - ระบบเคลมบริการหลังการขาย EV Services

## ภาพรวมระบบ (System Overview)

```mermaid
flowchart TB
    subgraph SC["ศูนย์บริการ (สาขา)"]
        SC1[สร้างเคลมใหม่]
        SC2[บันทึกแบบร่าง]
        SC3[ส่งอนุมัติ]
        SC4[แก้ไขข้อมูล]
        SC5[ดาวน์โหลด PDF]
    end
    
    subgraph AD["ผู้ดูแลระบบ (Admin)"]
        AD1[ตรวจสอบเคลม]
        AD2[อนุมัติ]
        AD3[ปฏิเสธ]
        AD4[ขอข้อมูลเพิ่ม]
        AD5[ดูรายงาน]
        AD6[Export Excel]
    end
    
    SC1 --> SC2
    SC2 --> SC3
    SC3 --> AD1
    AD1 --> AD2
    AD1 --> AD3
    AD1 --> AD4
    AD4 --> SC4
    SC4 --> SC3
    AD2 --> SC5
```

---

## Flow 1: การสร้างและส่งเคลม (สาขา)

```mermaid
flowchart TD
    A[เริ่มต้น] --> B[เข้าเมนู 'สร้างเคลมใหม่']
    B --> C[กรอกข้อมูลลูกค้า]
    C --> D[กรอกข้อมูลรถยนต์]
    D --> E{เป็นเคลมตรวจเช็คระยะ?}
    E -->|ใช่| F[เลือกระยะ + กรอกไมล์]
    E -->|ไม่ใช่| G[ข้าม]
    F --> H[กรอกรายละเอียด + จำนวนเงิน]
    G --> H
    H --> I[แนบเอกสาร]
    I --> J{เลือกการบันทึก}
    J -->|บันทึก| K["สถานะ: แบบร่าง"]
    J -->|บันทึกและส่งอนุมัติ| L["สถานะ: รออนุมัติ"]
    K --> M[สามารถแก้ไขได้]
    L --> N[ส่งไปยัง Admin]
    N --> O[จบ]
    M --> O
```

---

## Flow 2: การตรวจสอบและอนุมัติเคลม (Admin)

```mermaid
flowchart TD
    A[Admin เข้าเมนู 'อนุมัติเคลม'] --> B[เห็นรายการเคลมรออนุมัติ]
    B --> C[กดปุ่ม 'ตรวจสอบ']
    C --> D[ตรวจสอบข้อมูล]
    D --> E{ตัดสินใจ}
    
    E -->|อนุมัติ| F[กดปุ่ม 'อนุมัติ']
    F --> G[กรอกหมายเหตุ - ไม่บังคับ]
    G --> H[กด 'ยืนยัน']
    H --> I["สถานะ: อนุมัติแล้ว ✅"]
    I --> J[สาขาสามารถดาวน์โหลด PDF ได้]
    
    E -->|ปฏิเสธ| K[กดปุ่ม 'ปฏิเสธ']
    K --> L["กรอกเหตุผล (บังคับ)"]
    L --> M[กด 'ยืนยัน']
    M --> N["สถานะ: ปฏิเสธ ❌"]
    N --> O[สาขาเห็นเหตุผลการปฏิเสธ]
    
    E -->|ขอข้อมูลเพิ่ม| P[กดปุ่ม 'ขอข้อมูลเพิ่ม']
    P --> Q["กรอกข้อมูลที่ต้องการ (บังคับ)"]
    Q --> R[กด 'ยืนยัน']
    R --> S["สถานะ: ขอข้อมูลเพิ่ม 🔄"]
    S --> T[ไปยัง Flow 3]
```

---

## Flow 3: การแก้ไขและส่งเคลมใหม่ (กรณีขอข้อมูลเพิ่ม)

```mermaid
flowchart TD
    A["สถานะ: ขอข้อมูลเพิ่ม 🔄"] --> B[สาขาเห็นสถานะในรายการเคลม]
    B --> C[กดดูรายละเอียดเคลม]
    C --> D[เห็นหมายเหตุจาก Admin]
    D --> E[กดปุ่ม 'แก้ไข']
    E --> F[แก้ไขข้อมูลตามที่ Admin ต้องการ]
    F --> G{เลือกการบันทึก}
    
    G -->|บันทึก| H["สถานะ: ขอข้อมูลเพิ่ม (ยังไม่ส่ง)"]
    H --> I[สามารถแก้ไขเพิ่มได้]
    
    G -->|บันทึกและส่งอนุมัติ| J["สถานะ: รออนุมัติ"]
    J --> K[ส่งกลับไปยัง Admin]
    K --> L[Admin ตรวจสอบใหม่]
    L --> M[กลับไป Flow 2]
```

---

## Flow 4: การดาวน์โหลดเอกสารอนุมัติ (PDF)

```mermaid
flowchart TD
    A["สถานะ: อนุมัติแล้ว ✅"] --> B{ผู้ใช้}
    
    B -->|สาขา| C[เข้าหน้ารายละเอียดเคลม]
    C --> D[กดปุ่ม 'ดาวน์โหลดเอกสารอนุมัติ']
    
    B -->|Admin| E[เข้าหน้ารายละเอียดเคลม]
    E --> F[กดปุ่ม 'พิมพ์เอกสารอนุมัติ']
    
    D --> G[เปิด PDF ในแท็บใหม่]
    F --> G
    G --> H{ดำเนินการ}
    H -->|พิมพ์| I[กด Print]
    H -->|บันทึก| J[กด Download/Save]
```

---

## Flow 5: การดูรายงานและ Export Excel (Admin)

```mermaid
flowchart TD
    A[Admin เข้าเมนู 'รายงาน'] --> B[เลือก Filter]
    B --> C[เลือกสถานะ]
    C --> D[เลือกช่วงวันที่]
    D --> E[กดปุ่ม 'ค้นหา']
    E --> F[แสดงข้อมูลในตาราง]
    F --> G{ต้องการ Export?}
    G -->|ใช่| H[กดปุ่ม 'Export Excel']
    H --> I[ดาวน์โหลดไฟล์ .xlsx]
    G -->|ไม่| J[ดูข้อมูลในหน้าจอ]
```

---

## State Diagram: สถานะเคลม

```mermaid
stateDiagram-v2
    [*] --> แบบร่าง: สร้างเคลม + บันทึก
    
    แบบร่าง --> รออนุมัติ: ส่งอนุมัติ
    แบบร่าง --> แบบร่าง: แก้ไข
    
    รออนุมัติ --> อนุมัติแล้ว: Admin อนุมัติ
    รออนุมัติ --> ปฏิเสธ: Admin ปฏิเสธ
    รออนุมัติ --> ขอข้อมูลเพิ่ม: Admin ขอข้อมูลเพิ่ม
    
    ขอข้อมูลเพิ่ม --> ขอข้อมูลเพิ่ม: แก้ไข + บันทึก
    ขอข้อมูลเพิ่ม --> รออนุมัติ: แก้ไข + ส่งอนุมัติใหม่
    
    อนุมัติแล้ว --> [*]: จบ
    ปฏิเสธ --> [*]: จบ
```

---

## ตารางสรุป Actions ของแต่ละ Role

### ศูนย์บริการ (สาขา)

| Action | สถานะเดิม | สถานะใหม่ | คำอธิบาย |
|--------|-----------|-----------|----------|
| สร้างเคลม + บันทึก | - | แบบร่าง | สร้างเคลมใหม่ บันทึกเป็นแบบร่าง |
| สร้างเคลม + ส่งอนุมัติ | - | รออนุมัติ | สร้างและส่งอนุมัติทันที |
| แก้ไข + บันทึก | แบบร่าง | แบบร่าง | แก้ไขแบบร่าง |
| แก้ไข + ส่งอนุมัติ | แบบร่าง | รออนุมัติ | ส่งแบบร่างไปอนุมัติ |
| แก้ไข + บันทึก | ขอข้อมูลเพิ่ม | ขอข้อมูลเพิ่ม | แก้ไขแต่ยังไม่ส่ง |
| แก้ไข + ส่งอนุมัติ | ขอข้อมูลเพิ่ม | รออนุมัติ | ส่งกลับไป Admin ใหม่ |
| ดาวน์โหลด PDF | อนุมัติแล้ว | - | ดาวน์โหลดเอกสาร |

### ผู้ดูแลระบบ (Admin)

| Action | สถานะเดิม | สถานะใหม่ | คำอธิบาย |
|--------|-----------|-----------|----------|
| อนุมัติ | รออนุมัติ | อนุมัติแล้ว | อนุมัติเคลม |
| ปฏิเสธ | รออนุมัติ | ปฏิเสธ | ปฏิเสธเคลม (ต้องระบุเหตุผล) |
| ขอข้อมูลเพิ่ม | รออนุมัติ | ขอข้อมูลเพิ่ม | ส่งกลับให้สาขาแก้ไข |
| พิมพ์เอกสาร | อนุมัติแล้ว | - | เปิด PDF |
| Export Excel | - | - | ดาวน์โหลดรายงาน |

---

## Sequence Diagram: กรณี ขอข้อมูลเพิ่ม

```mermaid
sequenceDiagram
    participant SC as ศูนย์บริการ
    participant SYS as ระบบ
    participant AD as Admin
    
    SC->>SYS: สร้างเคลม + ส่งอนุมัติ
    SYS->>SYS: สถานะ = รออนุมัติ
    SYS-->>AD: แจ้งเตือนเคลมใหม่
    
    AD->>SYS: ตรวจสอบเคลม
    AD->>SYS: กด "ขอข้อมูลเพิ่ม"
    AD->>SYS: ระบุ "ต้องการรูปถ่ายเพิ่ม"
    SYS->>SYS: สถานะ = ขอข้อมูลเพิ่ม
    SYS-->>SC: แจ้งเตือนต้องแก้ไข
    
    SC->>SYS: เข้าดูเคลม
    SC->>SYS: เห็นหมายเหตุ "ต้องการรูปถ่ายเพิ่ม"
    SC->>SYS: กด "แก้ไข"
    SC->>SYS: เพิ่มรูปถ่าย
    SC->>SYS: กด "บันทึกและส่งอนุมัติ"
    SYS->>SYS: สถานะ = รออนุมัติ
    SYS-->>AD: แจ้งเตือนเคลมแก้ไขแล้ว
    
    AD->>SYS: ตรวจสอบเคลมอีกครั้ง
    AD->>SYS: กด "อนุมัติ"
    SYS->>SYS: สถานะ = อนุมัติแล้ว
    SYS-->>SC: แจ้งเตือนอนุมัติแล้ว
    
    SC->>SYS: เข้าดูเคลม
    SC->>SYS: กด "ดาวน์โหลดเอกสารอนุมัติ"
    SYS-->>SC: ส่ง PDF
```

---

*เอกสารนี้อัพเดทล่าสุด: มกราคม 2569*
